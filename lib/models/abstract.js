// import encode from './encode.js'
// import persist from './persist.js'

export class Abstract {

    constructor() {

        // Object.keys(options)

        //     .map(key => {

        //         encode[key] = options[key]
        //         persist[key] = options[key]

        //     })

        // if (options.dev)
        //     console.log('Running storagify in development mode')

        // if (options.debug)
        //     console.log('Running storagify in debug mode')

    }

}