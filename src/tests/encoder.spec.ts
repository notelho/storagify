

describe("testing encoder functions", () => {



  it("sums two numbers", () => {

    const
      word = 'testing',
      encoded = '',
      decoded = '';

    expect(encoded).toEqual(decoded);

  });



});
