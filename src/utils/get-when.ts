import Storagify from "../models/storagify";

export function getWhen(instance: Storagify, key: string): Date | null {

    //         const
    //             base = this._translate(instance).base,
    //             value = base.getItem(`${consts.devkey}${key}`),
    //             parsed = this._parse(value)
    //         return new Date(parsed.timestamp)



}

export default getWhen;